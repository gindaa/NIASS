<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="idev.gin.nias">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUpActivity"
            android:label="SIGN UP" />
        <activity
            android:name=".Form15Activity"
            android:label="FORM 15" />
        <activity
            android:name=".MenuKaderActivity"
            android:label="MENU KADER" />
        <activity
            android:name=".MenuNakesActivity"
            android:label="MENU NAKES" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps_activity" />
        <activity
            android:name=".ScoringActivity"
            android:label="SCORING" />
        <activity
            android:name=".Form16Activity"
            android:label="REGISTRASI KONTAK TUBERKOLOSIS" />
        <activity
            android:name=".KasusTbActivity"
            android:label="KASUS TB" />
        <activity
            android:name=".NotifikasiActivity"
            android:label="NOTIFIKASI" />
        <activity
            android:name=".PoinActivity"
            android:label="POIN" />
        <activity
            android:name=".ProfileAcivity"
            android:label="PROFILE" />
        <activity android:name=".RiwayatTbActivity"></activity>
    </application>

</manifest>